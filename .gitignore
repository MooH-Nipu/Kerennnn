.vercel
node_modules/

# Environment (secrets) — keep .env.example as the template
.env
.env.*
!.env.example

# AI / IDE tooling — local dev only
CLAUDE.md
.claude/
.wolf/
.cursor/
.codegraph/
.reasonix/

# Internal design roadmap — local only, not for the repo
ROADMAP.md
